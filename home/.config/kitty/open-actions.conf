# What kitty does when you CMD+click a link in a pane.
# Image files preview inline in an overlay instead of opening Preview.app;
# any key closes the overlay.
protocol file
mime image/*
action launch --type=overlay --title image kitten icat --hold --align center ${FILE_PATH}
